source "https://rubygems.org"
git_source(:github) { |repo| "https://github.com/#{repo['/'] ? repo : "mobius-network/#{repo}"}.git" }

gem "mobius-client", github: 'mobius-client-ruby'

gem "dry-initializer"
gem "celluloid-io"
gem "slack-ruby-client"
gem "telegram-bot-ruby"
gem "redis"
gem "i18n"

group :development do
  gem "rubocop"
end
